name: Test job failure status
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          token: ${{ github.token }}
          pr_branch_name: main
          actual_sha: ${{ github.sha }}
          run_attempt: "2"
          job_status: failure
        continue-on-error: true
      - name: Assert action ran with failure job status
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          # The action may fail due to missing Stoat config or API errors,
          # but it should not produce an unhandled exception (outcome is set).
          if [ -z "$OUTCOME" ]; then
            echo "FAIL: action outcome was not set"
            exit 1
          fi
          echo "PASS: action ran with failure job_status and produced an outcome: $OUTCOME"
